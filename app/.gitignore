# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env vars (Supabase URL/anon key) — anon key is safe for client bundles but
# keep it out of git anyway; .env.example documents the shape.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Supabase CLI local project link/state (machine-specific, not secret but not portable)
supabase/.temp
supabase/.branches
